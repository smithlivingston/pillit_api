// import express from 'express';
// import { register, trial } from '../middleware/authentication'

// export default (router :express.Router) => {
//         router.post('auth/register', register)
//         router.get('auth/trial', trial)
//         return router;
// }